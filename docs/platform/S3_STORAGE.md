# Supabase S3-compatible storage

| Key | Value |
|-----|--------|
| Endpoint | `https://ymhiwerqyegvondndkjn.storage.supabase.co/storage/v1/s3` |
| Region | `ap-northeast-1` |
| Bucket | `open-box-files` |
| Path style | true |

Railway service has `S3_*` env vars set for tooling and drivers.

In Open-Box UI: **Storages → Add → S3** (or compatible) using the same endpoint/region/keys from Railway variables / Supabase dashboard.
